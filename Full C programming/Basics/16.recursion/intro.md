What is recursion ?
-> Recursion is a meathod where a function calls itself again inside body, so that a larger problem can be broken down into simpler sub-problems.

How it works:
-> Base case:
This is the condition that prevent recursion otherwise it will infinte loop.

-> recursice loop:
The function calls itself with a simple output.

Advantages:
-> Easier to code and understand, between for trees (data structure)

Disadvantages:
-> Takes up more memory (stack space) and can be slower than a loop