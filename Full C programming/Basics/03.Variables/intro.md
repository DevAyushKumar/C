What is variable ? Full explanations.
-> Varaible are names given to data that no need to store and manipulate in our programs.

Note:- It refers to memory locations

Variable naming rules:
1. Must begain with a letter A-Z or a-z or underscore (_)
2. Can cointain letters, numbers, digits and underscores
3. Cannot be a c keyword
4. Case sensitive
5. No special character or spaces allowed

Global Variable:
-> A variable declared outside all functions are accessiable throughout the function. 

Not: It can be modified 

Static Variable:
-> Static variable is only initialized once and its value is preserved until the end of the program.

Note:
-> Declared with keyword static
-> retians the value between the function calls