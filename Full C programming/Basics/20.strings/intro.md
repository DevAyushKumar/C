What is string ?
-> String represents sequence of cahr values terminated by a null character ('\0')

syntax:
-> Datatype var-name [size]
-> Datatype var-name = "size"
-> Datatype var-name[size] = "size"

Note:
C language automatically adds '\0' at end.
